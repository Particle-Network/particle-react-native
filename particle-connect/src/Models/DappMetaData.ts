export class DappMetaData {
    url: string
    icon: string
    name: string

    constructor(url: string, icon: string, name: string) {
            this.url = url
            this.icon = icon
            this.name = name
        }
} 